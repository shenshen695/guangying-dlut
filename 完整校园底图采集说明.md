# 完整校园底图采集说明

## 首选：取得地图页面链接或原始资源

1. 在 i大工“校园地图服务系统”右上角点击 `…`。
2. 查找“复制链接”“在浏览器打开”“分享”或类似入口。
3. 把链接发给项目维护者，通过网页网络请求判断底图是单张大图还是地图瓦片。
4. 单张大图可直接下载；瓦片可按行列下载后无损合并。

这种方式不会出现截图按钮遮挡、缩放误差和拼接接缝，也是最适合正式网页的方案。使用前应确认学校地图素材的授权范围。

## 备选：固定缩放截图拼接

现有三张截图只覆盖横向的部分区域，且图层、信息、定位和导航按钮遮住了地图，暂不足以得到完整底图。继续采集时：

1. 固定手机方向、地图旋转角度和缩放等级，中途不要缩放。
2. 从校园图的左上角开始，按“从左到右、从上到下”的网格顺序截图。
3. 相邻截图至少保留 35% 重叠；建议采集 3–4 列、3–4 行。
4. 同一位置多拍一张轻微平移版本，让后一张补上前一张被按钮遮挡的区域。
5. 保留手机原始截图，不要经过微信压缩或裁切，并按 `row-01-col-01` 方式编号。

收齐后可统一裁掉顶部导航栏和底部手势条，屏蔽浮动按钮，再通过重叠建筑特征做透视校正与拼接。

## 编辑器中的使用

点位编辑器右上角“试用新底图”可以临时加载取得的整图或拼接图，用现有点位进行位置校准。确认后再把底图替换到项目中。
