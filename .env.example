# 当前静态 Demo 不读取此变量。
# 后续新增 Netlify Function 或其他服务端接口时，在部署平台后台配置真实值。
DEEPSEEK_API_KEY=

# 天气接口（当前使用 wttr.in，无需 API Key）
# 如需切换至 OpenWeatherMap 等付费服务，在此配置 Key，并在 app/api/weather/route.ts 中启用
WEATHER_API_KEY=
# 天气城市（默认：大连）
WEATHER_CITY=大连
